# Array
A small, header-only library for working with static and dynamic arrays of arbitrary dimension. Includes various memory layouts, views, iterators, and expression templates.

The next steps before adding new features are mainly optimization to match the performance of hand-written loops (iteration is generic and slow now), and some refactoring around traits.

Also needs a benchmark program and a more comprehensive program with examples.
